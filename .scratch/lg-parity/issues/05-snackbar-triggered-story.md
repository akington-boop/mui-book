Status: ready-for-human

## What to build

Add a Triggered Story for MUI Snackbar at `Components/Feedback/Snackbar`. The story includes a Button that triggers the snackbar — the snackbar should not render open by default.

## Acceptance criteria

- [ ] `Snackbar.tsx` wrapper exists under `src/Components/`
- [ ] `Snackbar.stories.tsx` filed at `Components/Feedback/Snackbar`
- [ ] Story renders a Button trigger that opens the snackbar
- [ ] 2–4 props (e.g. `anchorOrigin`, `autoHideDuration`, `message`) wired as interactive controls
- [ ] Renders correctly under the House Theme in light and dark mode

## Blocked by

None - can start immediately
