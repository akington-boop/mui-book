Status: ready-for-human

## What to build

Add a Component Story for MUI TextField at `Components/Inputs/TextField`. Create a wrapper component that picks 2–4 commonly used TextField props plus a `label` string, following the established wrapper pattern.

## Acceptance criteria

- [ ] `TextField.tsx` wrapper exists under `src/Components/`
- [ ] `TextField.stories.tsx` filed at `Components/Inputs/TextField`
- [ ] 2–4 props (e.g. `variant`, `size`, `disabled`, `multiline`) wired as interactive controls
- [ ] Renders correctly under the House Theme in light and dark mode

## Blocked by

None - can start immediately
