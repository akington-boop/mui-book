Status: ready-for-human

## What to build

Add a Component Story for MUI Tooltip at `Components/Data Display/Tooltip`. Tooltip requires a child element to anchor to — the wrapper should render a Button as the anchored child so the tooltip is visible in the story.

## Acceptance criteria

- [ ] `Tooltip.tsx` wrapper exists under `src/Components/`
- [ ] `Tooltip.stories.tsx` filed at `Components/Data Display/Tooltip`
- [ ] 2–4 props (e.g. `title`, `placement`, `arrow`) wired as interactive controls
- [ ] Renders correctly under the House Theme in light and dark mode

## Blocked by

None - can start immediately
