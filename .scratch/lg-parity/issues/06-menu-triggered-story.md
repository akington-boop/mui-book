Status: ready-for-human

## What to build

Add a Triggered Story for MUI Menu at `Components/Navigation/Menu`. The story includes a Button that acts as the anchor element — clicking it opens the menu. The menu should not render open by default.

## Acceptance criteria

- [ ] `Menu.tsx` wrapper exists under `src/Components/`
- [ ] `Menu.stories.tsx` filed at `Components/Navigation/Menu`
- [ ] Story renders a Button anchor that opens and closes the menu
- [ ] 2–4 props (e.g. `anchorOrigin`, `transformOrigin`, `dense`) wired as interactive controls
- [ ] Renders correctly under the House Theme in light and dark mode

## Blocked by

None - can start immediately
