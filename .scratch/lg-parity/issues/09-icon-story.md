Status: ready-for-human

## What to build

Add an Icon Story at `Components/Data Display/Icon`. The story demonstrates a single MUI icon (e.g. Star) rendered at all four sizes (large, medium, small, inherit) in a row, with a note that the Rounded `@mui/icons-material` variant is preferred. Scoped to `@mui/icons-material` only.

## Acceptance criteria

- [ ] `Icon.tsx` wrapper exists under `src/Components/`
- [ ] `Icon.stories.tsx` filed at `Components/Data Display/Icon`
- [ ] Story renders the icon at all four sizes: `large`, `medium`, `small`, `inherit`
- [ ] Story includes a note that the Rounded variant is preferred
- [ ] Renders correctly under the House Theme in light and dark mode

## Blocked by

None - can start immediately
