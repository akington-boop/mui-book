Status: ready-for-human

## What to build

Add a Fixed-Layout Story for MUI Card at `Components/Surfaces/Card`. Sub-part content (title, body text, action button) is hardcoded — only top-level Card props are exposed as interactive controls.

## Acceptance criteria

- [ ] `Card.tsx` wrapper exists under `src/Components/`
- [ ] `Card.stories.tsx` filed at `Components/Surfaces/Card`
- [ ] Card renders with hardcoded CardContent (title + body text) and a CardActions button
- [ ] 2–4 top-level props (e.g. `raised`, `variant`) wired as interactive controls
- [ ] Renders correctly under the House Theme in light and dark mode

## Blocked by

None - can start immediately
