Status: ready-for-human

## What to build

Add a Component Story for MUI Select at `Components/Inputs/Select`. Create a wrapper component that picks 2–4 commonly used Select props plus a `label` string, following the established wrapper pattern. Wire up the story with argTypes for each exposed prop.

## Acceptance criteria

- [ ] `Select.tsx` wrapper exists under `src/Components/`
- [ ] `Select.stories.tsx` filed at `Components/Inputs/Select`
- [ ] 2–4 props (e.g. `variant`, `size`, `disabled`, `multiple`) wired as interactive controls
- [ ] Renders correctly under the House Theme in light and dark mode

## Blocked by

None - can start immediately
